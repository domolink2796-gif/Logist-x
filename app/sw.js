/**
 * Минимальный Service Worker для прохождения проверки PWA.
 * Он не кэширует файлы (чтобы ты мог мгновенно обновлять код на GitHub),
 * но сообщает браузеру, что приложение готово к работе.
 */

self.addEventListener('install', (event) => {
    // Активируем воркер сразу после установки
    self.skipWaiting();
});

self.addEventListener('fetch', (event) => {
    // Просто пропускаем все запросы через воркер
    // Этого достаточно, чтобы кнопка в PWABuilder стала активной
});
